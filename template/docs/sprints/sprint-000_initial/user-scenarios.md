# {Project Name} — 用户场景

> **负责人**: @{PM_name}
> **TL 评审**: @{TL_name}
> **最后更新**: {YYYY-MM-DD}

---

## 1. 用户旅程图

### 旅程: {场景名称}

```
用户进入 {页面}
    │
    ├──→ {分支 A: 状态/条件}
    │       │
    │       └──→ {动作} → {结果}
    │
    └──→ {分支 B: 状态/条件}
            │
            └──→ {动作} → {结果}
```

## 2. 用例清单

| ID | 场景 | 前置条件 | 触发动作 | 预期结果 | 关联 Spec |
|:--:|:----|:--------|:--------|:--------|:---------|
| UC-01 | {场景} | {用户状态} | {UI 操作} | {系统响应} | SPEC-XXX |
| UC-02 | {场景} | {用户状态} | {UI 操作} | {系统响应} | SPEC-XXX |

## 3. 边界与异常

| 场景 | 异常条件 | 预期系统行为 |
|:----|:--------|:------------|
| UC-01 | {余额不足} | {显示错误提示，按钮恢复} |
| UC-01 | {网络断开} | {显示"网络错误"，无脏数据} |
| UC-02 | {合约拒绝} | {捕获错误，显示"交易被拒绝"} |
| 通用 | {用户超过 5 分钟无操作} | {session 超时，返回首页} |
