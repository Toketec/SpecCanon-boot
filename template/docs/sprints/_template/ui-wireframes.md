# SPRINT-{NNN}: {冲刺名称} — UI 线框图

> **负责人**: PM / 产品经理
> **用途**: 本次迭代涉及页面的布局和组件描述。替代 Figma 原型。
> **⚠️ 不写技术实现细节** — 只描述组件布局、状态、交互。

---

## 页面 1: {页面名称} — 路由: /{path}

### 布局

```
┌─────────────────────────────────────────────────┐
│  Header: {Logo} | {导航 1} | {导航 2}             │
├─────────────────────────────────────────────────┤
│  {功能区域描述}                                    │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐ │
│  │ {卡片 1}    │  │ {卡片 2}    │  │ {卡片 3}    │ │
│  │ {内容}      │  │ {内容}      │  │ {内容}      │ │
│  └────────────┘  └────────────┘  └────────────┘ │
├─────────────────────────────────────────────────┤
│  {页脚 / 分页 / 加载更多}                          │
└─────────────────────────────────────────────────┘
```

### 组件清单

| ID | 名称 | 类型 | 描述 | 状态集 | 交互 |
|:--:|:----|:----|:----|:------|:----|
| C1 | {组件名} | {按钮/列表/卡片} | {用途} | default/hover/disabled | 点击→{结果} |
| C2 | {组件名} | {输入框} | {用途} | empty/focused/filled/error | 输入→验证 |

### 页面状态

| 状态 | 显示内容 | 触发条件 |
|:----|:--------|:--------|
| 加载中 | 骨架屏 / loading 旋转 | 页面初次加载 |
| 空状态 | "还没有{内容}" + 引导按钮 | 数据为空 |
| 错误 | 错误提示 + 重试按钮 | API 请求失败 |
| 正常 | 完整页面内容 | 数据加载成功 |

---

## 页面 2: {页面名称} — 路由: /{path}

{同上格式}
